# CNN from scratch

This was my attempt at programming a CNN only using Python and NumPy.

The model achieves ~50% training and test accuracy after being trained on a small portion of the CIFAR-10 dataset.
